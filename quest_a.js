/**
 * ? Code 200
 * ! Die Anfrage wurde erfolgreich bearbeitet.
 *
 * ? 201 (Created)
 * ! Nach einem POST-Befehl zeigt der Statuscode Erfolg an.
 *
 * ? Code 204 (No Response)
 * ! Der Server hat die Anfrage erhalten, aber er bekommt keine Informationen zum Senden zurück.
 *
 * ? Code 400 (Bad Request)
 * ! Die Anfrage hatte eine falsche Syntax.
 *
 * ? 401 (Unauthorized)
 * ! Der Client hat sich nicht korrekt authentifiziert und hat daher keine Zugriffsberechtigung.
 *
 * ? 403 (Forbidden)
 * ! Die Anfrage ist für den Server verboten
 *
 * ? 404 (Not Found)
 * ! Der Server hat nichts unter der angegebenen URL(URI) gefunden.
 *
 * ? 500 (Internal Error)
 * ! Server hat einen unerwarteten Zustand festgestellt, der ihn daran gehindert hat, die Anfrage zu erfüllen.
 *
 * ? 503 (Service Unavailable)
 * ! Der Server kann die Anfrage aufgrund einer vorübergehenden Überlastung nicht bearbeiten.
 */
